package org.dmoreno.draw;

import org.dmoreno.figures.Figure;
import org.dmoreno.group.Group;

public class Draw {

    /**
     * This method Draws all the Figures in the different groups
     * @param grp1 Group of Figures to draw
     * @param grps You can draw more groups at the same time
     */
    public Draw(Group grp1, Group... grps){
        int idx;
        for (idx=0; idx < grps.length; idx++) {

        }
    }

    /**
     * This method is used to Draw only the Figure is passed
     * @param fig Figure to draw
     */

    public Draw(Figure fig) {

    }
}
